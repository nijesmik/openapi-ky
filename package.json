{
  "name": "openapi-ky-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@nijesmik/eslint-config": "^0.2.0",
    "@nijesmik/prettier-config": "^0.1.2",
    "@types/node": "^25.6.0",
    "eslint": "^9.39.1",
    "lefthook": "^2.1.6",
    "prettier": "^3.7.4",
    "typescript": "5.9.3",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
